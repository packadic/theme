builder: jekyll # which of the builders in grunt/builders to use
dest: dev     # any folder, relative to project root
type: dev     # dist|dev. dist will minify, optimize and complement tasks with extra stuff (time consuming stuff)
data:
  _config:
    name: Packadic
    description: Packadic theme
    keywords: [ webdesign,programming,designing ]

    url: http://packadic.radic.nl
    encoding: utf-8
    timezone: Amsterdam/Europe

    # GITHUB METADATA
    #https://github.com/jekyll/github-metadata
    repository: packadic/packadic.github.io
