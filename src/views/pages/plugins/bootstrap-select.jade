extends layout

block header
    +page-title("Bootstrap Select", "")
    +page-breadcrumbs
        +breadcrumb("Plugins")
        +breadcrumb("Bootstrap Select", "/plugins/bootstrap-select", true)

block plugin-panel-overview
    +plugin-info("Bootstrap Select", "bootstrap-select")

block plugin-example
    p To be done

block plugin-code
    p To be done
