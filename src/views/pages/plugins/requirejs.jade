extends layout

block header
    +page-title("RequireJS", "")
    +page-breadcrumbs
        +breadcrumb("Plugins")
        +breadcrumb("RequireJS", "/plugins/requirejs", true)

block plugin-panel-overview
    +plugin-info("RequireJS", "requirejs")

block plugin-example
    p To be done

block plugin-code
    p To be done
