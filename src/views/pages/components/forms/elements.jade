extends ../../../layouts/default

block header
    +page-title("Forms", "forms")
    +page-breadcrumbs
        +breadcrumb("Components", "/components")
        +breadcrumb("Forms")
        +breadcrumb("Layouts", "/components/forms/layouts", true)

block content
    .row
        .col-lg-12
            +box("Forms", "list")
                .row
                    .col-md-12
                        | Hello
