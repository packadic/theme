define([ 'jquery', 'eventer' ],
    function( $, eventer ){
        'use strict';

        var packadic = {};
        eventer('packadic', packadic);

        (function Initialisers(){
            packadic.init = function(options){

            }
        }.call());

        packadic.autoload = function(){

        };

        packadic.alert = function(){

        };

        packadic.notify = function(){

        };

        packadic.util = function(){

        };


        return packadic;
    });
